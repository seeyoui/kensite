<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.seeyoui.kensite.framework.system.persistence.SysMenuMapper" >
	<!-- 查询单表的字段 -->
	<sql id="tableColumns">
	    t.id,
	    t.parentid,
	    t.name,
	    t.url,
	    t.sequence,
	    t.icon,
	    t.target
	</sql>
	<!-- 查询过滤条件 -->
	<sql id="whereStr">
		<where>
			1=1
			<if test="id != null and id != ''">
				and t.id LIKE '%'||#{id}||'%'
			</if>
			<if test="parentid != null and parentid != ''">
				and t.parentid LIKE '%'||#{parentid}||'%'
			</if>
			<if test="name != null and name != ''">
				and t.name LIKE '%'||#{name}||'%'
			</if>
			<if test="url != null and url != ''">
				and t.url LIKE '%'||#{url}||'%'
			</if>
			<if test="sequence != null and sequence != ''">
				and t.sequence = #{sequence}
			</if>
			<if test="icon != null and icon != ''">
				and t.icon LIKE '%'||#{icon}||'%'
			</if>
			<if test="target != null and target != ''">
				and t.target LIKE '%'||#{target}||'%'
			</if>
		</where>
	</sql>
	<sql id="orderBy">
		ORDER BY t.sequence ASC 
	</sql>
	
	<!-- 根据ID查询单条数据 -->
	<select id="findSysMenuById" resultType="com.seeyoui.kensite.framework.system.domain.SysMenu">
		SELECT
		<include refid="tableColumns" />
		FROM SYS_MENU t
		WHERE id = #{id}
	</select>
	
	<!-- 查询数据集合 -->
	<select id="findSysMenuList" resultType="com.seeyoui.kensite.framework.system.domain.SysMenu">
		select * from(select t.*,rownum rn from(
			SELECT
			<include refid="tableColumns"/>
			FROM SYS_MENU t
			<include refid="whereStr"/>
			<include refid="orderBy"/>
		) t where rownum &lt; = #{page}*#{rows}) where rn &gt; (#{page}-1)*#{rows}
	</select>
	
	<!-- 查询数据总数 -->
	<select id="findSysMenuListTotal" resultType="EasyUIDataGrid">
		SELECT
		COUNT(id)total
		FROM SYS_MENU t
		<include refid="whereStr"/>
	</select>
	
	<!-- 查询数据TREE -->
	<select id="findSysMenuTree" resultType="com.seeyoui.kensite.framework.system.domain.SysMenu" parameterType="com.seeyoui.kensite.framework.system.domain.SysUser">
		SELECT 
		<include refid="tableColumns"/>
		from sys_user su
		left join sys_user_role sur
		on su.id = sur.userid
		<if test="username !=null and username !=''">
		and su.username=#{username}
		</if>
		left join sys_role sr
		on sur.roleid = sr.id
		left join sys_role_menu srm
		on sr.id = srm.roleid
		left join sys_menu t
		on srm.menuid = t.id
		ORDER BY t.sequence ASC
	</select>
	
	<!-- 查询数据TREE -->
	<select id="getTreeJson" resultType="com.seeyoui.kensite.framework.system.domain.SysMenu" parameterType="com.seeyoui.kensite.framework.system.domain.SysUser">
		SELECT 
		<include refid="tableColumns"/>
		from 
		sys_menu t
		ORDER BY t.sequence ASC
	</select>
	
	<!-- 数据新增 -->
	<insert id="saveSysMenu" parameterType="com.seeyoui.kensite.framework.system.domain.SysMenu">
		INSERT INTO SYS_MENU (
	    	
	    	parentid,
	    	name,
	    	url,
	    	sequence,
	    	icon,
	    	target,
		    id
		)
		VALUES (
			
			#{parentid},
			#{name},
			#{url},
			#{sequence},
			#{icon},
			#{target},
		    #{id}
		)
	</insert>
	
	<!-- 数据修改 -->
	<update id="updateSysMenu" parameterType="com.seeyoui.kensite.framework.system.domain.SysMenu">
		UPDATE SYS_MENU
		<set>
	    	
	    	<if test="parentid !=null and parentid !=''">
			parentid=#{parentid},
			</if>
	    	<if test="name !=null and name !=''">
			name=#{name},
			</if>
	    	<if test="url !=null and url !=''">
			url=#{url},
			</if>
	    	<if test="sequence !=null and sequence !=''">
			sequence=#{sequence},
			</if>
	    	<if test="icon !=null and icon !=''">
			icon=#{icon},
			</if>
	    	<if test="target !=null and target !=''">
			target=#{target},
			</if>
		    id = #{id}
		</set>
		WHERE id = #{id}
	</update>
	
	<!-- 数据删除 -->
	<delete id="deleteSysMenu">
		DELETE FROM SYS_MENU WHERE id IN 
		<foreach item="listId" collection="list"
			open="(" separator="," close=")">
			#{listId}
		</foreach>
	</delete>
</mapper>